/*
 * 1️⃣ Cadastro de Alunos com Notas
 *
 * Crie uma classe `Aluno` com os atributos:
 * - `nome`
 * - `matricula`
 * - `notas` (um array de 3 notas)
 *
 * Métodos:
 * ✔️ Construtor para inicializar o aluno.
 * ✔️ `exibirInformacoes()` → Exibe os dados do aluno e suas notas.
 * ✔️ `calcularMedia()` → Percorre o array de notas e retorna a média.
 * ✔️ `verificarAprovacao()` → Verifica se a média do aluno é maior ou igual a 7.
 *
 * No `main()`, crie um array de `Aluno` para armazenar 3 alunos,
 * peça ao usuário para preencher os dados e depois exiba as informações.
 */

/*
 * 2️⃣ Loja de Produtos
 *
 * Crie uma classe `Produto` com os atributos:
 * - `nome`
 * - `preco`
 * - `quantidadeEmEstoque`
 *
 * Métodos:
 * ✔️ `exibirProduto()` → Mostra os detalhes do produto.
 * ✔️ `adicionarEstoque(int quantidade)` → Adiciona unidades ao estoque.
 * ✔️ `removerEstoque(int quantidade)` → Remove unidades do estoque (se houver disponível).
 *
 * No `main()`, crie um array de 5 produtos e permita ao usuário manipular o estoque dos produtos.
 */

/*
 * 3️⃣ Biblioteca (Livros)
 *
 * Crie uma classe `Livro` com os atributos:
 * - `titulo`
 * - `autor`
 * - `anoPublicacao`
 *
 * Crie uma classe `Biblioteca` que contém um array de `Livro` e os métodos:
 * ✔️ `adicionarLivro(Livro livro)` → Adiciona um livro ao array.
 * ✔️ `listarLivros()` → Exibe todos os livros cadastrados.
 * ✔️ `buscarPorAutor(String autor)` → Percorre o array e retorna os livros de um autor específico.
 *
 * No `main()`, crie uma `Biblioteca`, adicione livros e permita que o usuário pesquise por autor.
 */

/*
 * 4️⃣ Cadastro de Funcionários
 *
 * Crie uma classe `Funcionario` com os atributos:
 * - `nome`
 * - `salario`
 * - `cargo`
 *
 * Métodos:
 * ✔️ `exibirDados()` → Mostra as informações do funcionário.
 * ✔️ `aumentarSalario(double percentual)` → Aumenta o salário de acordo com um percentual informado.
 *
 * No `main()`, crie um array de 4 funcionários, permita ao usuário cadastrar,
 * listar e aumentar o salário de um funcionário específico.
 */

/*
 * 5️⃣ Time de Futebol
 *
 * Crie uma classe `Jogador` com os atributos:
 * - `nome`
 * - `posicao`
 * - `numeroCamisa`
 *
 * Crie uma classe `Time` que contém um array de `Jogador` e os métodos:
 * ✔️ `adicionarJogador(Jogador jogador)` → Adiciona um jogador ao time.
 * ✔️ `listarJogadores()` → Exibe os jogadores cadastrados.
 * ✔️ `buscarPorPosicao(String posicao)` → Retorna os jogadores que jogam na mesma posição.
 *
 * No `main()`, crie um `Time`, cadastre jogadores e permita buscar jogadores por posição.
 */
